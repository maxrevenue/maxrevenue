# ── Build output ──────────────────────────────────────────────────────────
build/
.gradle/

# ── JARs / binaries (client packs + agent artifacts are large & derivable) ─
*.jar
*.class
# ...except the Gradle wrapper jar, which is conventionally committed.
!gradle/wrapper/gradle-wrapper.jar

# ── Decompiled client classes (generated, not source-of-truth) ────────────
/com/

# ── Temp / generated ──────────────────────────────────────────────────────
_vidframes/
*.log

# ── OS / editor cruft ─────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# ── License Worker ────────────────────────────────────────────────────────
license-server/node_modules/
license-server/.wrangler/
license-server/.dev.vars
license-server/.prod.vars
license-server/worker-configuration.d.ts

# ── Ops / Discord / pricing (never publish) ───────────────────────────────
docs/discord-setup.md
tools/discord-setup.mjs
**/discord-bot-token*
**/.env
**/.env.*
