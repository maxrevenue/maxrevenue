package com.automation.core.model;

/** Prayers the decision engine cares about. Deliberately small and client-neutral. */
public enum Prayer {
    PROTECT_FROM_MELEE,
    PROTECT_FROM_MISSILES,
    PROTECT_FROM_MAGIC,
    PIETY,
    RIGOUR,
    AUGURY,
    SMITE,
    REDEMPTION
}
