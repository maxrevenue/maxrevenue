# Local Cloudflare Worker secrets — copy to .dev.vars (gitignored).
# TOKEN_SECRET must match LicenseToken.HMAC_SECRET in the Java client.
ADMIN_SECRET=change-me-local-admin
TOKEN_SECRET=change-me-match-LicenseToken-HMAC_SECRET
